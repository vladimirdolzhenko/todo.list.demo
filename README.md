# Todo list Spring demo app

## Running the Application

Start the database:

```bash
$ docker-compose up -d
```
### Build and run the application:

```bash
mvn clean install
mvn spring-boot:run
```

## Access the application:

http://localhost:8080
